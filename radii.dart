import 'package:flutter/material.dart';

class AppRadii {
  static const borderRadiusRadiusXxsm = BorderRadius.all(Radius.circular(2));

  static const borderRadiusRadiusXl = BorderRadius.all(Radius.circular(999));

  static const borderRadiusRadiusXsm = BorderRadius.all(Radius.circular(4));

  static const borderRadiusRadiusSm = BorderRadius.all(Radius.circular(8));

  static const borderRadiusRadiusMd = BorderRadius.all(Radius.circular(12));

  static const borderRadiusRadiusLg = BorderRadius.all(Radius.circular(16));

  AppRadii._();
}
